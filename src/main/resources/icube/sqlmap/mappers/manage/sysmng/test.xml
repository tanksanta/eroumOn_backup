<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">

	<select id="selectAllTest" resultType="java.util.HashMap">
	SELECT 
		test_no
		, test_nm
		, data
	FROM TEST;
	</select>

	<insert id="insertMngTest" parameterType="java.util.HashMap">
	/* Query ID : test.xml > insertMngTest */
	INSERT INTO TEST (
		test_nm
		, data
	) VALUES (
	#{test_nm, jdbcType=VARCHAR}
	, #{data, jdbcType=VARCHAR}
	)
	</insert>


	<update id="updateMngTest" parameterType="java.util.HashMap">
		/* Query ID : test.xml > updateMngTest */
		UPDATE TEST SET
			data = #{data, jdbcType=VARCHAR}
		WHERE test_nm = #{test_nm}
	</update>

</mapper>